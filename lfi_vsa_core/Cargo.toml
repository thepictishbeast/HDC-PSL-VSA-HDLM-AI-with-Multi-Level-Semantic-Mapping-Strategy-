[package]
name = "lfi_vsa_core"
version = "0.1.0"
edition = "2021"
description = "Localized Forensic Intelligence — 10,000-dim Bipolar VSA HDC Core"

[dependencies]
bitvec = "1.0.1"
rand = "0.8.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
