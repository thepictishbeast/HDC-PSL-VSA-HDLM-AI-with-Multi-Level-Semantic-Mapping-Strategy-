#!/usr/bin/expect -f
set timeout 30
log_user 1

spawn sh -c "cargo run --bin chat 2>&1"
expect "Enter Password>"
send -- "$env(LFI_SOVEREIGN_KEY)"
expect "User>"

send "who was the first person on the moon?\r"
expect "User>"

send "exit\r"
expect eof
